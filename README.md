.EDU TLD Mail Best Practices
===========================

A staggering number of .edu domains are very poorly configured.

Many don't meet the bare-minimum, e-mail best practices of SPF records (let alone sane records without `?all` or huge netblocks).

I'm not sure *any* use DMARC or a site-wide _domainkey policy. Let's see!
